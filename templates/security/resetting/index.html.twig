{% extends 'base.html.twig' %}

{% block body %}

    <h1>Renouvellement du mot de passe</h1>

    {{form_start(form)}}
            {{ form_row(form.password,{'label': 'Mot de passe', 'attr' : {'placeholder' : 'Entrez votre Mot de passe...' }} ) }}
            {{ form_row(form.ConfirmPassword, {'label': ' Comfirmation Mot de passe', 'attr' : {'placeholder' : 'Répétez votre Mot de passe...' }} ) }}
            <button type="submit" class="btn btn-success">Enregistrer</button>
    {{form_end(form)}}

{% endblock body %}